# Dual SIM Call Log Selection Implementation

## Steps:
- [x] 1. Add `getAvailableSims()` method to `lib/services/call_log_service.dart`
- [x] 2. Update `lib/screens/call_logs_screen.dart`: Add `_showSimSelectionModal()`, standardize SIM labels
- [ ] 3. Modify `_loadData()` to fetch logs then show mandatory SIM modal
- [ ] 4. Update filter logic and dropdown (remove 'All', mandatory selection)
- [ ] 5. Test: `flutter run` on dual-SIM device, verify modal/selection/filtering

**Completed:** None yet  
**Next:** Step 2

